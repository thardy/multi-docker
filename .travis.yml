sudo: required
services:
  - docker

before_install:
  - docker build -t thardy/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true USERNAME/react-test npm test

after_success:
  - docker build -t thardy/multi-client ./client
  - docker build -t thardy/multi-nginx ./nginx
  - docker build -t thardy/multi-server ./server
  - docker build -t thardy/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker r_github_packages:
  - docker push thardy/multi-client
  - docker push thardy/multi-nginx
  - docker push thardy/multi-server
  - docker push thardy/multi-worker



